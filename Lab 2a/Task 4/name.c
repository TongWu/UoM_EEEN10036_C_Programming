#include <stdio.h>
int main(void) {
    int times10;
    //int 10times; identifier cannot begin as a number
    int _done;
    int _10times;
    //int user name; identifer cannot contain spaces
    //int user.name; identifier cannot contain point
    int user_name;
    int username;
    printf("Legal identifiers:\n&d\n%d\n%d\n%d\n%d\n%d\n%d\n%d\n", times10, _done, _10times, user_name, username);
    return 0;
}